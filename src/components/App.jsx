import React, { Component } from 'react';

class App extends Component {
  state = {
    contacts: [],
    name: '',
  };

  render() {
    const { good, neutral, bad } = this.state;
    return <div>help</div>;
  }
}

export default App;
